server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/preorder?characterEncoding=UTF-8&serverTimezone=UTC
    username: taek
    password: 1234


  symmetric:
    key: symmetricUiTaekKeysymmetricUiTaekKeysymmetricUiTaekKeysymmetricUiTaekKeysymmetricUiTaekKeysymmetricUiTaekKeysymmetricUiTaekKey
  #jpa
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      #jpa ddl-auto
      #create: DROP ? CREATE
      #create-drop: DROP ? CREATE ? DROP, DML? ???? ?? ?? DROP?? ??
      #update: Entity? DB ??? ?? ? ?? ?? ??(?? ??)
      #validate: Entity? DB ??? ??(??? ?? ??)
      ddl-auto: create

    #jpa format
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  jwt:
    secret: uiTaekSecretKeyuiTaekSecretKeyuiTaekSecretKeyuiTaekSecretKeyuiTaekSecretKeyuiTaekSecretKeyuiTaekSecretKey
  #log
  output:
    ansi:
      enabled: always

  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 1024MB
  #mail
  mail:
    host: smtp.gmail.com
    port: 587
    username: uitaeklym027@gmail.com
    password: fkhg owto xshn jvjf
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
